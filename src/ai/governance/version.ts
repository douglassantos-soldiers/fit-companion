/** Bump when governance audit / metrics / eval semantics change. */
export const AI_GOVERNANCE_VERSION = "governance_v1";

/** Bump when AiAuditEvent / diagnostic view shape changes. */
export const AI_GOVERNANCE_CONTRACT_VERSION = 1;

/** Postgres persist schema label (FASE 11). */
export const AI_AUDIT_PERSIST_VERSION = "persist_v1";
