/**
 * MCP social domain — placeholder (no tools in this phase).
 * Future read tools must use social-read.server with TrustedIdentity.
 */
export function registerSocialTools(): void {
  // Intentionally empty — avoid unsafe social reads without membership checks.
}
